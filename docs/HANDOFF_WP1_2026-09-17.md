# WP0 / WP1 handoff — 2026-09-17

**Boundary:** WP0's minimum freeze and WP1's candidate-probe layer are implemented and locally reproduced. Stop here for independent audit. No E001 training or subsequent work packet is authorized by this handoff.

## Exact repository state

| Checkpoint | Identity |
| --- | --- |
| Branch | `codex/wp0-wp1-candidate-probe` |
| Clean starting main | `c918252d7ce0ba308dba36e509a1363b21f3811f` |
| Pre-observation freeze and implementation | `25638aea30bda4b4a5ef9fb0dd8e5193a578a584` |
| Initial invocation source, after path-test correction | `5888d2c4d805655f6b5e97adcb536b4abb1d0a5f` |
| Final invocation source, after accounting repair | `7adfeb9ef5ce928d0ba1568d33d3702694d2f63f` |
| Complete evidence/results checkpoint | `03dce2cfe6300a51b870155e66644d51111467b2` |

This handoff is a documentation-only child of the complete evidence checkpoint. Resolve its own exact commit with `git log -1 --format=%H -- docs/HANDOFF_WP1_2026-09-17.md`; a file cannot embed its own commit hash. The reviewed implementation is exactly `7adfeb9`; later commits preserve evidence and documentation. No protected or pre-existing dirty files were present or altered.

`.venv/` and `.models/` are ignored local execution assets. The pinned 19 model/tokenizer/config/card files total 1,020,544,559 bytes; weights, downloaded custom code, caches and credentials are not tracked. Recreate them using the preparation instructions. No issue comments were posted and no issues were closed. Remote publication status must be checked against the branch ref, not inferred from this file.

**Delivery checkpoint:** the user explicitly approved publishing this branch, code, synthetic evidence and handoff to `JosephJMWalker-MBA/masi-research`. The approved push succeeded and the branch now tracks `origin/codex/wp0-wp1-candidate-probe`. An earlier push attempt was rejected by automatic approval review for unestablished publication authorization; it sent no branch update and was not bypassed. The first confirmed remote checkpoint was `4145ec1e80a58077c395873de0aa9d476dde70c3`; this subsequent documentation update records the publication. Verify the current remote ref for the latest handoff commit. No pull request was opened and no issue state changed.

## Active question and authority

Read and used `README.md`, `STATUS.md`, `AGENTS.md`, thesis, modules, evaluation, candidate registry, prior-art posture, contribution rules, experiment lifecycle and Sprint 001. GitHub Issues [#1](https://github.com/JosephJMWalker-MBA/masi-research/issues/1), [#2](https://github.com/JosephJMWalker-MBA/masi-research/issues/2), and [#4](https://github.com/JosephJMWalker-MBA/masi-research/issues/4) were read through the GitHub connector; all were open with zero comments at retrieval on 2026-09-17. Their broader acceptance remains open.

The frozen question is whether a deterministic baseline and two heterogeneous local implementations can use unchanged calling code on identical bounded premise/claim inputs while preserving native outputs, normalized decisions, failures and resource provenance. It tests an engineering seam underlying H4, not efficacy of the architecture.

## Decisions fixed before model observation

- `wp1-support-v1` specifies the common envelope and selected-file freeze hashes. The scope is binary premise/claim support; `not_supported` is not equivalent to contradiction.
- Inputs are nonempty strings, at most 4,096 characters each; models abstain above 512 formatted input tokens without truncation. Candidate adapters receive no fixture label/category. One invocation, no retries, retrieval, tools or training.
- Exact-match baseline: trimmed casefold equality gives support, else abstention; score unavailable. Learned support/entailment score strictly above/below 0.5 maps to support/non-support, equality abstains. No calibration claim.
- HHEM revision `8e4a2e6e96c708cc76c2344f7e4757df2515292c`; foundation tokenizer/config `7bcac572ce56db69c1ea7c8af255c5d7c9672fc2`; NLI `9a77395d4d3751be9e2a69c4ae318491d9b3fffb`. All three pinned cards declare Apache-2.0. HHEM's reviewed custom loader is redirected to its pinned local foundation instead of a floating Hub dependency.
- CPU float32, batch one, one intra/inter-op thread, seed zero, deterministic algorithms, fixed dependency lock. Same text but different tokenization is recorded.
- Twelve visible author-created synthetic fixtures, six support and six non-support references, no held-out split. Correctness uses all twelve as denominator; abstentions/errors do not count as correct. Acceptance depends on interface/evidence integrity and repeatability within `1e-6`, not accuracy gains.
- Native and normalized outputs remain separate and hash-linked; failures remain first-class and evidence directories refuse overwrite. Disagreement is not reconciled or given authority.
- MASI Bus was inspected at `b9d8d632f7a3d1b4528d80bfad6c743e4e3ed490`. WP1 does not duplicate its schema or fabricate its required confidence. Reality Audit minimum is documented only; no action/outcome learner was built.

## Implementation and changed files

- `probe.py`: fixed-fixture caller, validation, normalization, run artifacts, descriptive scoring and resources.
- `probe_candidates.py`: fixed three-candidate table, pinned metadata, deterministic rule and two local classifier adapters.
- `scripts/prepare_probe_models.py`: bounded pinned downloads; `scripts/verify_probe_evidence.py`: six-run integrity/repeat checker.
- `tests/test_probe.py`: 22 standard-library tests, no model downloads or real inference.
- `.gitignore`, `requirements-probe.lock`: isolated dependencies and excluded weights/caches.
- `docs/WP0_WP1_CONTRACT.md`; narrow crosslinks in thesis/modules/evaluation; status/checkpoint updates in README, STATUS, sprint and registry.
- `experiments/wp1/`: protocol, fixtures, freeze, usage/prior-art record, results, supporting review and both complete six-run evidence sets.

The exact file inventory through the evidence checkpoint is reproducible with:

```sh
git diff --name-only c918252d7ce0ba308dba36e509a1363b21f3811f 03dce2cfe6300a51b870155e66644d51111467b2
```

## Commands and validation

Setup executed with Python 3.11.15:

```sh
/opt/homebrew/bin/python3.11 -m venv .venv
.venv/bin/python -m pip install --no-cache-dir 'torch==2.8.0' 'transformers==4.57.6' 'sentencepiece==0.2.1' 'protobuf==6.33.5'
.venv/bin/python -m pip freeze > requirements-probe.lock
.venv/bin/python scripts/prepare_probe_models.py --model-root .models
```

The initial sandboxed package download failed DNS resolution. An authorized network retry completed; preparation succeeded at exact revisions. Host preflight observed arm64, 16 GiB and 10 logical CPUs. Network access is used only for setup/source retrieval; model invocation uses local-only loaders and offline flags.

Final validation commands and outcomes:

```sh
.venv/bin/python -m unittest discover -s tests -v
# PASS: 22 tests; also independently rerun by supporting reviewer.
.venv/bin/python -m pip check
# PASS: No broken requirements found.
.venv/bin/python -m compileall -q probe.py probe_candidates.py scripts tests
# PASS.
git diff --check
# PASS.
.venv/bin/python scripts/verify_probe_evidence.py
# PASS: original six runs, all three pairs maximum native difference 0.
.venv/bin/python scripts/verify_probe_evidence.py --evidence experiments/wp1/evidence/accounting-repair
# PASS: final six runs, all three pairs maximum native difference 0.
```

The invocation command is `.venv/bin/python probe.py --candidate CANDIDATE --output DIRECTORY`. Actual initial mappings were `exact-match-v1 -> experiments/wp1/evidence/baseline-{1,2}`, `hhem-2.1 -> experiments/wp1/evidence/hhem-{1,2}`, and `nli-deberta-small -> experiments/wp1/evidence/nli-{1,2}`. Each brace denotes two separate invocations. Final invocations used the same names beneath `experiments/wp1/evidence/accounting-repair/`. Runs were sequential; no inference concurrency or extra passes were used. Never rerun into an existing directory.

Final test output is in `experiments/wp1/evidence/accounting-repair/tests-22.log`; dependency check in `experiments/wp1/evidence/dependencies.log`; complete final loader warnings in `experiments/wp1/evidence/accounting-repair/inference.log`. Each run's manifest includes source hashes, exact Git identity, environment, model file hashes and output hashes. The only untracked path at final invocation was its accumulating evidence directory; no uncommitted code was invoked.

## Observed result and failures preserved

The baseline answered three fixtures correctly and abstained on nine. HHEM and NLI each answered all twelve and matched all twelve reference labels. No model run had an execution error. Native outputs and normalized decisions reproduced exactly within both six-run sets, and the cross-version check found no prediction changes after the accounting repair. Total retained: 12 runs, 144 fixture records, 96 learned forward calls. This is repeated execution on one visible fixture set, not independent-dataset replication.

Negative/error paths are exercised through fake-only tests, not described as observed model failures. A macOS temporary-path alias assertion initially failed and was repaired before inference. A later transient indentation error in a test edit was caught and corrected before the final passing commit. The supporting reviewer found loss of valid completed-call counts after normalization errors; this was fixed and retested, retaining original run artifacts. No criteria, labels, thresholds or candidate revisions changed after observation.

Upstream HHEM emits a model-type naming warning; transformers emits a dtype deprecation warning. These remain in logs and did not block execution. The pinned setup works on this host; upgrades and other hosts remain unverified. No hosted CI was executed.

## Unknowns and claims not supported

No held-out task quality, independent label agreement, calibration, generalization, training gain, composition value, governance benefit, efficient routing, outcome learning or frontier competitiveness is established. The synthetic fixture set is tiny and intentionally simple. Different pretraining, model sizes, formatting and token counts remain confounds for capability/efficiency claims. Both learned implementations are text classifiers, not evidence of broad responsibility diversity. Energy and amortized cost are unknown; process RSS and wall latency are descriptive.

The guard freezes selected files; the runner is source-hashed per run rather than automatically included in that guard. Future changes must preserve source identity and the frozen semantics. Broader contract transport integration, cross-role contracts and actual Reality Audit outcomes remain undefined for execution. E001 split/labels/metrics/base selection and all later work packets remain open.

## Audit and exact next action

Supporting same-family review found no blocking defect in successful paths and returned **ACCEPT scoped to the call-accounting repair** after independently rerunning 22 tests. See `experiments/wp1/SUPPORTING_REVIEW.md`. This is not the planned independent Claude audit and is not full work-packet acceptance.

**Next action:** hand this branch to the independent auditor. Have them read the frozen contract/protocol, inspect the diff from `c918252`, verify both evidence sets with the commands above, and review raw versus normalized records, fairness, licensing, failure accounting, source identity and claim limits. They should record one of `ACCEPT`, `REPAIR_REQUIRED`, `CLAIM_NARROWING_REQUIRED`, `INCONCLUSIVE`, or `RESEARCH_QUESTION_DEFECT` for the complete WP0/WP1 packet. If accepted, select a separately authorized WP2/E001 design packet; do not begin training or broaden the runtime from this handoff alone.

---

## Appended reconciliation annotations — R3, R5 and R7 (2026-09-18)

Everything above this separator is preserved byte-for-byte from [the historical handoff at `81adfff`](https://github.com/JosephJMWalker-MBA/masi-research/blob/81adfffa2d28e4a40a0b36fe1a21baa567d402ec/docs/HANDOFF_WP1_2026-09-17.md). Its branch state, commands, assertions and next action describe that historical packet, not this documentation-only checkout. This annotation is based on governing `main` at `a0ac9fe9cd400a6c03d6543d5b79f651bf89a3a5` and [Claude's independent audit](AUDIT_WP0_WP1_2026-09-17.md), whose outcome is `CLAIM_NARROWING_REQUIRED`. The frozen builder artifact, code and evidence have not been modified or merged here. These annotations supersede interpretation and next-action wording where specified below; they do not rewrite the historical claims.

### R3 — E001 framing supersession

The historical references to E001 "base selection" and "training" do not define the target architecture. Under current doctrine and Issue #3's governing framing, E001 targets **purpose-built bounded Empathy-observation intelligence designed from the responsibility outward**. Any pretrained base selection or fine-tuning is an optional `CONTROL_B` arm, not the presumed specialist. E001 preparation, labeling, split freezing, base selection, implementation and training remain unauthorized by this reconciliation. ADCP's deterministic downstream policy remains outside the observation target.

### R5 — closed-set invocation result

The historical "two heterogeneous local implementations" and unchanged-caller/interchangeability descriptions are narrowed as follows: under `wp1-support-v1`, three pre-registered implementations — a deterministic equality floor (`INFRASTRUCTURE`/B0) and two externally adapted pretrained transformer classifiers (`CONTROL_B`-class specimens) — used uniform invocation by changing only the candidate identifier and reproduced bit-identically on one host. **Normalization is caller-resident and candidate-keyed.** Substitution is demonstrated for that closed registered set, not arbitrary new implementations or purpose-built targets with different native ontologies. The protocol's inference-branching failure condition was not triggered; the contract's broader caller-branch condition holds only for invocation, not normalization. The learned candidates differ in architecture/objective within the same HF/PyTorch classifier stack and one adapter class; cross-paradigm interchangeability is unestablished.

The [implementation-class record](../experiments/wp1/IMPLEMENTATION_CLASSES.md) covers every artifact category. No `MASI_TARGET` or `CONTROL_A` was observed, and the externally adapted specimens are not H2A evidence without a same-base comparison. The frozen contract's H1–H7 matrix is WP1-local and superseded for thesis purposes by current [RESEARCH_THESIS.md](RESEARCH_THESIS.md): H2A/H2B, `C_A/C_B/T` controls and falsifiers 1–10 govern. Issue #1's WP0 thesis-freeze deliverable remains open.

The historical reference-match counts establish no capability deficit of transparent or rule-based Precision methods. The equality floor is deliberately non-competitive, and the learned counts do not justify learned complexity. Fixtures were visible, author-created with candidates known, and cover canonical NLI phenomena overlapping the NLI specimen's documented training mixture (audit N7); this confound rules out a capability interpretation.

### R7 — erratum to the historical `git diff --check` PASS claim

The original command and `# PASS.` line in "Commands and validation" remain visible and unchanged above. The bare `git diff --check` validated **only an empty working-tree diff**, not the complete builder packet. The packet-level command `git diff --check c918252 81adfff` reports two trailing-whitespace lines at `docs/WP0_WP1_CONTRACT.md:3-4`. Those lines are intentional Markdown hard breaks in the frozen contract. The frozen file is intentionally left unedited. The historical pass is therefore limited to the command's actual empty-diff scope and must not be read as a packet-level whitespace pass.

### Audit status and next action supersession

Claude's independent audit is now complete with outcome `CLAIM_NARROWING_REQUIRED`; the historical "independent audit pending" statements above are superseded. R1–R7 are submitted as documentation-only narrowing, not self-acceptance of WP1. The exact next action is a **scoped Claude re-check of this reconciliation diff**, including the preserved handoff prefix, R7 erratum, unchanged protected artifacts and existing evidence verification. The doctrine hold stays in force. WP2/E001, new candidates, training, runtime changes and merging any branch require separate operator authorization and do not follow automatically from this handoff.

E1 remains a next-contract lesson, not a WP1 repair: the verifier shares helpers with the runner, and a future separately authorized contract may add an independent reference check. No evaluator, code or evidence change is authorized here. Other non-blocking observations in the audit remain unresolved where recorded, including the NLI freeze-attestation limit and incomplete training-lineage review; the narrative-only setup/indentation events above are historical self-reports, not retained evaluation evidence.
